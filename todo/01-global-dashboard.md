# Global Dashboard

Auto-generate `data/dashboard.md` (or `index.html`) after each sync: meetings-per-week
trend, top tags/keywords, total meetings processed, most active participants. Pure
aggregation of existing `state.json`/`vocabulary.json` data — no new LLM calls.
