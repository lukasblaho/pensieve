# Action-Item Tracker

Aggregate all `next_actions` across meetings into `data/action-items.json` +
`open-actions.md`. Detect carry-over/recurring items (same task text/owner appearing in a
later meeting) to flag stale or repeatedly-postponed tasks.
